{
  "template": "module Main where\nimport Control.Exception (try, SomeException, evaluate)\nimport System.IO (hSetBuffering, stdout, BufferMode(NoBuffering))\n\nfilterWith :: [a -> Bool] -> [a] -> [a]\nfilterWith predicates values = filter [[SLOT filter_lambda]] values\n\nmain :: IO ()\nmain = kataMain",
  "slots": [
    {
      "name": "filter_lambda",
      "answer": "(\\x -> all ($ x) predicates)",
      "hint": "lambda",
      "decoys": [
        "(\\x -> any ($ x) predicates)",
        "(\\x -> map ($ x) predicates)",
        "(\\p -> all p values)"
      ]
    }
  ],
  "prompt": "Pick the lambda that applies every predicate to a candidate."
}