{
  "template": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.26;\n\ncontract SimpleToken {\n    string public name;\n    string public symbol;\n    uint8 public [[SLOT decimals_init]];\n    uint256 public totalSupply;\n    mapping(address => uint256) public balanceOf;\n\n    event Transfer(address indexed from, address indexed to, uint256 value);\n\n    error InsufficientBalance();\n    error ZeroAddress();\n\n    constructor(string memory _name, string memory _symbol, uint256 _initialSupply) {\n        name = _name;\n        symbol = _symbol;\n        totalSupply = _initialSupply;\n        balanceOf[msg.sender] = _initialSupply;\n        emit Transfer(address(0), msg.sender, _initialSupply);\n    }\n\n    function transfer(address to, uint256 amount) external returns (bool) {\n        if ([[SLOT zero_check]]) revert ZeroAddress();\n        if ([[SLOT bal_check]]) revert InsufficientBalance();\n        \n        balanceOf[msg.sender] -= amount;\n        balanceOf[to] += amount;\n        [[SLOT transfer_event]]\n        return true;\n    }\n}\n",
  "slots": [
    {
      "name": "decimals_init",
      "answer": "decimals = 18",
      "hint": "literal",
      "decoys": [
        "decimals = 6",
        "decimals = 8",
        "decimals = 0"
      ]
    },
    {
      "name": "zero_check",
      "answer": "to == address(0)",
      "hint": "predicate",
      "decoys": [
        "to != address(0)",
        "to == msg.sender",
        "to == 0"
      ]
    },
    {
      "name": "bal_check",
      "answer": "balanceOf[msg.sender] < amount",
      "hint": "predicate",
      "decoys": [
        "balanceOf[msg.sender] <= amount",
        "balanceOf[to] < amount",
        "amount > balanceOf[msg.sender]"
      ]
    },
    {
      "name": "transfer_event",
      "answer": "emit Transfer(msg.sender, to, amount);",
      "hint": "event",
      "decoys": [
        "emit Transfer(to, msg.sender, amount);",
        "emit Transfer(amount, msg.sender, to);",
        "Transfer(msg.sender, to, amount);"
      ]
    }
  ],
  "prompt": "Pick the decimals, zero-address check, balance check, and Transfer emit."
}