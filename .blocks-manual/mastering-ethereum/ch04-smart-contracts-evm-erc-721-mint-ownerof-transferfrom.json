{
  "template": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.26;\n\ncontract SimpleNFT {\n    uint256 private _nextTokenId = 1;\n    mapping(uint256 => address) private _owners;\n\n    error TokenDoesNotExist();\n    error NotOwner();\n\n    function mint(address to) external returns (uint256) {\n        uint256 tokenId = [[SLOT id_inc]];\n        _owners[tokenId] = to;\n        return tokenId;\n    }\n\n    function ownerOf(uint256 tokenId) external view returns (address) {\n        address owner = _owners[tokenId];\n        if ([[SLOT exists_check]]) revert TokenDoesNotExist();\n        return owner;\n    }\n\n    function transferFrom(address from, address to, uint256 tokenId) external {\n        address owner = _owners[tokenId];\n        if (owner == address(0)) revert TokenDoesNotExist();\n        if ([[SLOT auth_check]]) revert NotOwner();\n        _owners[tokenId] = to;\n    }\n}\n",
  "slots": [
    {
      "name": "id_inc",
      "answer": "_nextTokenId++",
      "hint": "post-inc",
      "decoys": [
        "++_nextTokenId",
        "_nextTokenId += 1",
        "_nextTokenId"
      ]
    },
    {
      "name": "exists_check",
      "answer": "owner == address(0)",
      "hint": "predicate",
      "decoys": [
        "owner != address(0)",
        "tokenId == 0",
        "_owners[tokenId] == 0"
      ]
    },
    {
      "name": "auth_check",
      "answer": "msg.sender != from",
      "hint": "predicate",
      "decoys": [
        "msg.sender == from",
        "from != owner",
        "msg.sender != to"
      ]
    }
  ],
  "prompt": "Pick the post-increment, the existence check, and the from-vs-sender auth."
}