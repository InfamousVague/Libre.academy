{
  "template": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.26;\n\ncontract SimpleNFT {\n    uint256 public nextTokenId;\n    mapping(uint256 => address) public ownerOf;\n    mapping(uint256 => address) private _tokenApprovals;\n\n    event Approval(address indexed owner, address indexed approved, uint256 indexed tokenId);\n\n    error NotOwner();\n    error NonexistentToken();\n\n    function mint(address to) external returns (uint256) {\n        uint256 tokenId = nextTokenId;\n        ownerOf[tokenId] = to;\n        nextTokenId++;\n        return tokenId;\n    }\n\n    function approve(address approved, uint256 tokenId) external {\n        address owner = ownerOf[tokenId];\n        if (msg.sender != owner) revert NotOwner();\n        [[SLOT set_approval]]\n        [[SLOT approval_event]]\n    }\n\n    function getApproved(uint256 tokenId) external view returns (address) {\n        if ([[SLOT exists_check]]) revert NonexistentToken();\n        return _tokenApprovals[tokenId];\n    }\n}\n",
  "slots": [
    {
      "name": "set_approval",
      "answer": "_tokenApprovals[tokenId] = approved;",
      "hint": "set",
      "decoys": [
        "_tokenApprovals[approved] = tokenId;",
        "_owners[tokenId] = approved;",
        "tokenApprovals[tokenId] = approved;"
      ]
    },
    {
      "name": "exists_check",
      "answer": "ownerOf[tokenId] == address(0)",
      "hint": "predicate",
      "decoys": [
        "ownerOf[tokenId] != address(0)",
        "tokenId == 0",
        "_owners[tokenId] == address(0)"
      ]
    },
    {
      "name": "approval_event",
      "answer": "emit Approval(owner, approved, tokenId);",
      "hint": "event",
      "decoys": [
        "emit Approval(approved, owner, tokenId);",
        "emit Approval(msg.sender, approved, tokenId);",
        "Approval(owner, approved, tokenId);"
      ]
    }
  ],
  "prompt": "Pick the approval setter, the existence check, and the Approval event."
}