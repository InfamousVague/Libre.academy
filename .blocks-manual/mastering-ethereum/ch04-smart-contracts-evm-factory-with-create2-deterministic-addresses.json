{
  "template": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.20;\n\ncontract Child {\n    address public deployer;\n    constructor() { deployer = msg.sender; }\n}\n\ncontract Factory {\n    event Deployed(address indexed addr, bytes32 indexed salt);\n\n    function deploy(bytes32 salt) external returns (address addr) {\n        addr = address([[SLOT create2_call]]);\n        emit Deployed(addr, salt);\n    }\n\n    function predict(bytes32 salt) external view returns (address) {\n        bytes32 initCodeHash = [[SLOT init_hash]];\n        return address(uint160(uint256(\n            [[SLOT addr_hash]]\n        )));\n    }\n}\n",
  "slots": [
    {
      "name": "create2_call",
      "answer": "new Child{salt: salt}()",
      "hint": "create2",
      "decoys": [
        "new Child()",
        "new Child{value: 0}()",
        "new Child{salt: 0}()"
      ]
    },
    {
      "name": "init_hash",
      "answer": "keccak256(type(Child).creationCode)",
      "hint": "hash",
      "decoys": [
        "keccak256(Child.creationCode)",
        "type(Child).creationCode",
        "keccak256(type(Child).runtimeCode)"
      ]
    },
    {
      "name": "addr_hash",
      "answer": "keccak256(abi.encodePacked(bytes1(0xff), address(this), salt, initCodeHash))",
      "hint": "hash",
      "decoys": [
        "keccak256(abi.encode(bytes1(0xff), address(this), salt, initCodeHash))",
        "keccak256(abi.encodePacked(address(this), salt, initCodeHash))",
        "keccak256(abi.encodePacked(bytes1(0xff), salt, initCodeHash))"
      ]
    }
  ],
  "prompt": "Pick the CREATE2 call, the init-code hash, and the address-derivation hash."
}