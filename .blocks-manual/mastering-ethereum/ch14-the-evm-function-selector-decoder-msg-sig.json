{
  "template": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.26;\n\ncontract SelectorDecoder {\n    uint256 public callCount;\n    bytes4 public lastSelector;\n\n    function increment() external {\n        [[SLOT save_sig]]\n        callCount++;\n    }\n\n    function decrement() external {\n        [[SLOT underflow_check]]\n        lastSelector = msg.sig;\n        callCount--;\n    }\n\n    function reset() external {\n        lastSelector = msg.sig;\n        callCount = 0;\n    }\n\n    function getSelector(string calldata funcSig) external pure returns (bytes4) {\n        return [[SLOT selector_calc]];\n    }\n}\n",
  "slots": [
    {
      "name": "save_sig",
      "answer": "lastSelector = msg.sig;",
      "hint": "selector",
      "decoys": [
        "lastSelector = msg.data;",
        "lastSelector = bytes4(msg.data);",
        "msg.sig = lastSelector;"
      ]
    },
    {
      "name": "selector_calc",
      "answer": "bytes4(keccak256(bytes(funcSig)))",
      "hint": "hash",
      "decoys": [
        "bytes4(keccak256(funcSig))",
        "keccak256(bytes(funcSig))",
        "bytes4(funcSig)"
      ]
    },
    {
      "name": "underflow_check",
      "answer": "require(callCount > 0, \"Underflow\");",
      "hint": "guard",
      "decoys": [
        "require(callCount >= 0);",
        "require(callCount != 0);",
        "if (callCount == 0) revert();"
      ]
    }
  ],
  "prompt": "Pick the msg.sig save, the keccak-derived selector, and the underflow guard."
}