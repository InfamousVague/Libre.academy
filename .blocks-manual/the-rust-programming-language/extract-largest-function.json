{
  "template": "pub fn largest(list: [[SLOT slice_param]]) -> &i32 {\n    let mut largest = [[SLOT first_ref]];\n    for item in list {\n        if [[SLOT compare]] {\n            largest = item;\n        }\n    }\n    largest\n}\n\nfn main() {\n    let number_list = vec![34, 50, 25, 100, 65];\n    let result = largest(&number_list);\n    println!(\"The largest number is {result}\");\n\n    let number_list = vec![102, 34, 6000, 89, 54, 2, 43, 8];\n    let result = largest(&number_list);\n    println!(\"The largest number is {result}\");\n}\n",
  "slots": [
    {
      "name": "slice_param",
      "answer": "&[i32]",
      "hint": "type",
      "decoys": [
        "Vec<i32>",
        "&Vec<i32>",
        "[i32]"
      ]
    },
    {
      "name": "first_ref",
      "answer": "&list[0]",
      "hint": "init",
      "decoys": [
        "list[0]",
        "&list.first()",
        "list.first().unwrap()"
      ]
    },
    {
      "name": "compare",
      "answer": "item > largest",
      "hint": "predicate",
      "decoys": [
        "item >= largest",
        "largest > item",
        "*item > *largest"
      ]
    }
  ],
  "prompt": "Pick the &[i32] slice, the &list[0] init, and the item > largest predicate."
}