{
  "template": "/// Inline `greeter` module — single-file rewrite of the original\n/// lib.rs + main.rs split. The playground can't expose a custom\n/// crate name (it forces `package.name = \"playground\"`), so the\n/// \"another crate\" angle of the exercise becomes \"another module\"\n/// here. Same visibility / namespacing rules apply.\n[[SLOT mod_decl]] {\n    pub fn greet(name: &str) -> String {\n        [[SLOT fmt_call]]\n    }\n}\n\nfn main() {\n    let message = [[SLOT qualified_call]];\n    println!(\"{}\", message);\n}\n",
  "slots": [
    {
      "name": "mod_decl",
      "answer": "pub mod greeter",
      "hint": "module",
      "decoys": [
        "mod greeter",
        "pub crate greeter",
        "pub fn greeter"
      ]
    },
    {
      "name": "fmt_call",
      "answer": "format!(\"Hello, {}!\", name)",
      "hint": "format",
      "decoys": [
        "format!(\"Hello, {name}!\")",
        "format(\"Hello, {}!\", name)",
        "\"Hello, \".to_string() + name + \"!\""
      ]
    },
    {
      "name": "qualified_call",
      "answer": "greeter::greet(\"World\")",
      "hint": "path",
      "decoys": [
        "greeter.greet(\"World\")",
        "greet(\"World\")",
        "crate::greeter::greet(\"World\")"
      ]
    }
  ],
  "prompt": "Pick the pub-mod decl, the format! call, and the qualified call."
}