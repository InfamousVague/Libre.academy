{
  "template": "/// Returns the first word in the input string (up to the first space).\n/// If there is no space, returns the entire string.\npub fn first_word(s: &str) -> &str {\n    let bytes = [[SLOT as_bytes]];\n    for (i, &item) in [[SLOT iter_enum]] {\n        if item == [[SLOT byte_lit]] {\n            return [[SLOT slice_expr]];\n        }\n    }\n    &s[..]\n}\n\nfn main() {\n    let sentence = \"hello world\";\n    let word = first_word(sentence);\n    println!(\"First word: {}\", word);\n}",
  "slots": [
    {
      "name": "as_bytes",
      "answer": "s.as_bytes()",
      "hint": "convert",
      "decoys": [
        "s.bytes()",
        "s.to_bytes()",
        "s.chars()"
      ]
    },
    {
      "name": "iter_enum",
      "answer": "bytes.iter().enumerate()",
      "hint": "iter",
      "decoys": [
        "bytes.enumerate()",
        "bytes.iter().count()",
        "bytes.zip(0..)"
      ]
    },
    {
      "name": "byte_lit",
      "answer": "b' '",
      "hint": "literal",
      "decoys": [
        "' '",
        "0x20",
        "\" \""
      ]
    },
    {
      "name": "slice_expr",
      "answer": "&s[0..i]",
      "hint": "slice",
      "decoys": [
        "s[0..i]",
        "&s[..i]",
        "s.slice(0, i)"
      ]
    }
  ],
  "prompt": "Pick the as_bytes, iter+enumerate, byte literal, and the slice expression."
}