{
  "template": "fn main() {\n    let result = transform_input(\"  21  \");\n    println!(\"Result: {result}\");\n}\n\n/// Takes a string slice containing a number with possible whitespace,\n/// trims it, parses it to u32, doubles it, and returns the result.\npub fn transform_input(input: &str) -> u32 {\n    let input = [[SLOT trim_call]];\n    let input = [[SLOT parse_unwrap]];\n    let input = [[SLOT double]];\n    input\n}\n",
  "slots": [
    {
      "name": "trim_call",
      "answer": "input.trim()",
      "hint": "string",
      "decoys": [
        "input.strip()",
        "input.clean()",
        "input.trim_end()"
      ]
    },
    {
      "name": "parse_unwrap",
      "answer": "input.parse::<u32>().unwrap()",
      "hint": "parse",
      "decoys": [
        "input.parse::<i32>().unwrap()",
        "u32::parse(input).unwrap()",
        "input.to_u32().unwrap()"
      ]
    },
    {
      "name": "double",
      "answer": "input * 2",
      "hint": "math",
      "decoys": [
        "input + 2",
        "input * 2.0",
        "2 * input.value"
      ]
    }
  ],
  "prompt": "Pick the trim, parse-with-turbofish, and double-the-input expressions."
}