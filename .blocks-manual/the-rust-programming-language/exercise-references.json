{
  "template": "/// Returns the length of a String without taking ownership.\npub fn calculate_length([[SLOT ref_param]]) -> usize {\n    [[SLOT len_call]]\n}\n\nfn main() {\n    let s1 = String::from(\"hello\");\n    let len = calculate_length([[SLOT take_ref]]);\n    println!(\"The length of '{}' is {}.\", s1, len);\n}\n",
  "slots": [
    {
      "name": "ref_param",
      "answer": "s: &String",
      "hint": "param",
      "decoys": [
        "s: String",
        "s: &str",
        "s: &mut String"
      ]
    },
    {
      "name": "len_call",
      "answer": "s.len()",
      "hint": "method",
      "decoys": [
        "s.length()",
        "s.size()",
        "len(s)"
      ]
    },
    {
      "name": "take_ref",
      "answer": "&s1",
      "hint": "reference",
      "decoys": [
        "s1",
        "&mut s1",
        "s1.clone()"
      ]
    }
  ],
  "prompt": "Pick the &String parameter, the len() call, and the &s1 reference at the call site."
}