{
  "template": "pub struct Rectangle {\n    pub width: u32,\n    pub height: u32,\n}\n\npub fn make_square(size: u32) -> Rectangle {\n    Rectangle {\n        width: size,\n        height: size,\n    }\n}\n\npub fn area(rect: &Rectangle) -> u32 {\n    [[SLOT area_formula]]\n}\n\nfn main() {\n    let rect = Rectangle { width: 30, height: 50 };\n    println!(\"Area: {}\", area(&rect));\n\n    let sq = [[SLOT square_call]];\n    println!(\"Square area: {}\", area(&sq));\n\n    let wider = Rectangle { width: 60, [[SLOT struct_update]] };\n    println!(\"Wider area: {}\", area(&wider));\n}\n",
  "slots": [
    {
      "name": "area_formula",
      "answer": "rect.width * rect.height",
      "hint": "math",
      "decoys": [
        "rect.width + rect.height",
        "2 * (rect.width + rect.height)",
        "rect.width / rect.height"
      ]
    },
    {
      "name": "struct_update",
      "answer": "..rect",
      "hint": "update",
      "decoys": [
        "...rect",
        "rect",
        "&rect"
      ]
    },
    {
      "name": "square_call",
      "answer": "make_square(20)",
      "hint": "call",
      "decoys": [
        "make_square()",
        "make_square(20.0)",
        "Rectangle::square(20)"
      ]
    }
  ],
  "prompt": "Pick the area formula, the struct-update syntax, and the make_square call."
}