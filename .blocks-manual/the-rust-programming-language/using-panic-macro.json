{
  "template": "/// Panics with the given message.\n/// \n/// # Panics\n/// Always panics, printing `msg`.\npub fn crash_now(msg: &str) {\n    [[SLOT panic_call]]\n}\n\nfn main() {\n    // Uncomment to test locally:\n    // crash_now(\"something went wrong\");\n}",
  "slots": [
    {
      "name": "panic_call",
      "answer": "panic!(\"{}\", msg);",
      "hint": "macro",
      "decoys": [
        "panic!(msg);",
        "eprintln!(\"{}\", msg);",
        "unreachable!();"
      ]
    }
  ],
  "prompt": "Pick the panic! macro invocation."
}