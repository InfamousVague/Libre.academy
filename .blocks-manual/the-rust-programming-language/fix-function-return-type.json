{
  "template": "fn main() {\n    let result = double(21);\n    println!(\"double(21) = {result}\");\n}\n\n/// Returns twice the input value.\n[[SLOT fn_sig]] {\n    [[SLOT body]]\n}\n",
  "slots": [
    {
      "name": "fn_sig",
      "answer": "pub fn double(x: i32) -> i32",
      "hint": "signature",
      "decoys": [
        "pub fn double(x: i32)",
        "pub fn double(x: i32) -> ()",
        "pub fn double(x: i64) -> i32"
      ]
    },
    {
      "name": "body",
      "answer": "x * 2",
      "hint": "expr",
      "decoys": [
        "x + 2",
        "x * x",
        "return x * 2;"
      ]
    }
  ],
  "prompt": "Pick the i32→i32 signature and the body expression."
}