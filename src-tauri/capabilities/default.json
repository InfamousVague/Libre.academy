{
  "$schema": "../gen/schemas/capability.json",
  "identifier": "default",
  "description": "Default permissions for the Kata app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
